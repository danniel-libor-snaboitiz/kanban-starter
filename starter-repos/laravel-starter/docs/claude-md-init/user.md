# User CLAUDE.md (generated)

> Note: `/init` only generates a *project*-level CLAUDE.md — it scans a codebase.
> There is no `/init` for user-level preferences, so this file is the generic
> starter template Claude drafts when asked for a global CLAUDE.md, left largely
> unedited on purpose so it can be compared against the hand-written
> `docs/claude-md-manual/user.md`. See `docs/claude-md-notes.md`.

# Personal Preferences

This file contains global preferences that apply across all projects.

## Communication

- Provide clear, complete explanations of changes and reasoning.
- Ask clarifying questions when requirements are ambiguous.
- Summarize what was done at the end of a task.

## Coding Style

- Write clean, readable, well-documented code.
- Follow the conventions of the language and framework in use.
- Add comments to explain complex logic.
- Prefer descriptive variable and function names.

## Testing

- Write tests for new functionality.
- Ensure existing tests continue to pass.
- Aim for good test coverage.

## Version Control

- Write clear, descriptive commit messages.
- Make focused commits.
- Follow conventional commit format where applicable.

## General

- Prioritize correctness and maintainability.
- Consider edge cases and error handling.
- Keep security best practices in mind.
